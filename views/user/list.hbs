<h3><a class="btn btn-secondary" href="/user"> Create New</a> User List</h3>
<table class="table table-striped">
    Create New</a>
  User List</h3>
<table class='table table-striped'>
  <thead>
    <tr>
      <th>Full Name</th>
      <th>Email</th>
      <th>Mobile</th>
      <th>City</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
        {{#each list}}
        <tr>
            <td>{{this.fullName}}</td>
            <td>{{this.email}}</td>
            <td>{{this.mobile}}</td>
            <td>{{this.city}}</td>
            <td>
                <a href="/user/{{this._id}}">Edit | </i></a>
                <a href="/user/delete/{{this._id}}" onclick="return confirm('Are you sure to delete this record ?');"> | Del</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>